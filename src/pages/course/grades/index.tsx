import { Box, Typography } from "@mui/material";

const GradesPage = () => {
  return (
    <Box>
      <Typography component={"h2"}>Your Grades</Typography>
    </Box>
  );
};

export default GradesPage;
